<div class="container">
  <div class="search-bar d-flex">
    <%= form_tag items_path, method: :get, class:"full-search-bar" do %>
      <%= text_field_tag :query, params[:query], class: "form-control", placeholder: "Find an item" %>
      <%= submit_tag "Search", class: "btn btn-primary search-btn" %>
    <% end %>
  </div>


  <div class="row">
    <%= link_to "All", items_path(category: ""), method: :get, class: "category btn-primary" %>
    <%= link_to "Meat", items_path(category: "meat"), method: :get, class: "category btn-primary" %>
    <%= link_to "Seafood", items_path(category: "seafood"), method: :get, class: "category btn-primary" %>
    <%= link_to "Fruits", items_path(category: "fruit"), method: :get, class: "category btn-primary" %>
    <%= link_to "Vegetables", items_path(category: "vegetable"), method: :get, class: "category btn-primary" %>
    <%= link_to "Dried Goods", items_path(category: "dried goods"), method: :get, class: "category btn-primary" %>
  </div>
</div>

<div class="container d-flex">
  <div class="row col-9">
      <% @items.each do |item| %>
        <%= render 'shared/components/cards', item: item %>
      <% end %>
  </div>
  <div class="col-3">
    <div class="basket">

      <%#= image_tag "basket", alt: "basketimage" %>
      <div class="top-bskt">
        <i class="fas fa-shopping-basket icon-baskt"></i>
        <h2 class="yr-baskt">Your Basket</h2>
      </div>
      <div class="wish-items">
        <!-- here goes the iteration for the wish list items -->
        <% current_user.wish_list_items.each do |wish_item| %>
          <div class="d-flex justify-content-between names-icon">
            <div class="name-quantity">
              <p class="wish-item"><%= wish_item.item.generic_name.capitalize %></p>
              <p class="wish-item"><%= wish_item.item.generic_quantity * wish_item.amount %> <%= wish_item.item.generic_unit %></p>
            </div>
            <%= link_to plus_amount_wish_list_item_path(wish_item) do %>
              <p class="wish-item"><i class="fas fa-plus"></i></p>
            <% end %>
            <p class="wish-item"><%= wish_item.amount %></p>
            <%= link_to minus_amount_wish_list_item_path(wish_item) do %>
              <p class="wish-item"><i class="fas fa-minus"></i></p>
            <% end %>
            <%= link_to wish_list_item_path(wish_item), method: :delete do %>
              <p class="wish-item"><i class="fas fa-trash" id="bin"></i></p>
            <% end %>
          </div>
        <% end %>
      </div>
      <div class="con-btn">
        <%= link_to "Compare Results", compare_path, class: "btn btn-primary compare-button" %>
      </div>
      </div>
    </div>
</div>


